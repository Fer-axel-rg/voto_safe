spring.application.name=voto-safe-backend

# ==========================================
# 🚀 CONEXIÓN A BASE DE DATOS SUPABASE (CORREGIDA)
# ==========================================
# Usamos el puerto 6543 (que sabemos que tu red permite)
# Y agregamos prepareThreshold=0 (para arreglar el error 500 del Prepared Statement)
spring.datasource.url=jdbc:postgresql://aws-1-ca-central-1.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0
spring.datasource.username=postgres.gplwhcrmudbgcekgvbfp
spring.datasource.password=Senatinosunidos

# Driver de PostgreSQL
spring.datasource.driver-class-name=org.postgresql.Driver

# ==========================================
# ⚙️ JPA / HIBERNATE
# ==========================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# ==========================================
# 🏊 POOL DE CONEXIONES (HIKARI)
# ==========================================
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.connection-timeout=20000

# ==========================================
# 🤖 INTELIGENCIA ARTIFICIAL (OLLAMA)
# ==========================================
spring.ai.ollama.base-url=http://localhost:11434/
spring.ai.ollama.chat.options.model=llama3
spring.ai.ollama.chat.model=llama3

# ==========================================
# 🌐 SERVIDOR
# ==========================================
server.port=8080

# ⚠️ ¡IMPORTANTE!: Asegúrate de NO tener otra línea 'spring.datasource.url' debajo de esto.
# Si tienes la línea que pegaste al final anteriormente, BORRALA.