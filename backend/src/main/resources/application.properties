spring.application.name=voto-safe-backend

# ==========================================
# 🚀 CONEXIÓN A BASE DE DATOS SUPABASE
# ==========================================
# Usando la conexión a través del Pooler (recomendado)
spring.datasource.url=jdbc:postgresql://aws-1-ca-central-1.pooler.supabase.com:6543/postgres  
spring.datasource.username=postgres.gplwhcrmudbgcekgvbbfp  
spring.datasource.password=Senatinosunidos 

# Driver de PostgreSQL
spring.datasource.driver-class-name=org.postgresql.Driver

# ==========================================
# ⚙️ JPA / HIBERNATE (Se mantienen igual)
# ==========================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ==========================================
# 🏊 POOL DE CONEXIONES (HIKARI) - Puedes mantener esta configuración de pool si te funciona.
# ==========================================
# Configuración que funciona bien con el pooler de Supabase
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.connection-timeout=20000

# ==========================================
# 🤖 INTELIGENCIA ARTIFICIAL (OLLAMA)
# ==========================================
# Asegúrate de tener Ollama corriendo en otra terminal
spring.ai.ollama.base-url=http://localhost:11434
spring.ai.ollama.chat.options.model=llama3
spring.ai.ollama.chat.model=llama3

# ==========================================
# 🌐 SERVIDOR
# ==========================================
server.port=8080